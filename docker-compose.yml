services:
  app:
    build:
      context: .
      dockerfile: manifest/docker/Dockerfile
      args:
        VERSION: canary
    image: ghcr.io/rain-kl/igolib:canary
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: ${TZ:-Asia/Shanghai}
    ports:
      - "8000:8000"
    volumes:
      - ./data/uploads:/app/uploads
      - ./data/sqlite:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-wavelet}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      TZ: ${TZ:-Asia/Shanghai}
#    ports:
#      - "5432:5432"
    volumes:
      # PG18+ 镜像要求挂载父目录（数据实际落在 18/docker 子目录）；旧式
      # ...:/var/lib/postgresql/data 会被视作废弃挂载而拒绝启动。
      - ./data/postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-wavelet}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: valkey/valkey:8.0-alpine
    restart: unless-stopped
    command: ["valkey-server", "--appendonly", "yes"]
#    ports:
#      - "6379:6379"
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
#
#  jaeger:
#    image: jaegertracing/jaeger:${JAEGER_VERSION:-2.19.0}
#    restart: unless-stopped
#    environment:
#      TZ: ${TZ:-Asia/Shanghai}
#    ports:
#      - "${JAEGER_UI_PORT:-16686}:16686"
#      - "${JAEGER_OTLP_GRPC_PORT:-4317}:4317"
#      - "${JAEGER_OTLP_HTTP_PORT:-4318}:4318"
#
#  clickhouse:
#    image: clickhouse/clickhouse-server:25.3-alpine
#    restart: unless-stopped
#    profiles:
#      - clickhouse
#    environment:
#      CLICKHOUSE_DB: ${CLICKHOUSE_DB:-wavelet}
#      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-default}
#      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-123456}
#      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: 1
#      TZ: ${TZ:-Asia/Shanghai}
#    ports:
#      - "${CLICKHOUSE_HTTP_PORT:-8123}:8123"
#      - "${CLICKHOUSE_NATIVE_PORT:-9000}:9000"
#    volumes:
#      - ./data/clickhouse_data:/var/lib/clickhouse
#    healthcheck:
#      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
#      interval: 10s
#      timeout: 5s
#      retries: 5
#      start_period: 15s
